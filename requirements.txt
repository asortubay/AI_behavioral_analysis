# Core dependencies for AI Behavioral Analysis Pipeline
# Python version: 3.10.x

# Video sync + landmark extraction
numpy==2.1.2
scipy==1.14.1
opencv-python==4.10.0.84
mediapipe==0.10.11
tqdm==4.67.1

# Note: ffmpeg/ffprobe are required system tools (not a pip package).
